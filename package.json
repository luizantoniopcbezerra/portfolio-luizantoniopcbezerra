{
  "name": "portfolio",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch:projects": "node scripts/fetch-pinned.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
